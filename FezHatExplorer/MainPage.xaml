<Page
    x:Class="FezHatExplorer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FezHatExplorer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Available lightbulbs"/>
            <ListBox ItemsSource="{Binding FezHats}" Grid.Row="1" SelectedValue="{Binding SelectedFezHat,Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding DefaultAppName}"/>
                            <TextBlock Text="{Binding UniqueName}"/>
                            <TextBlock Text="{Binding ModelNumber}"/>
                            <TextBlock Text="{Binding DateOfManufacture}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <ToggleSwitch Grid.Row="2" Header="Light Status" Toggled="OnRedLedToggled"/>
        </Grid>
    </Page>

